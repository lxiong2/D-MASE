globalState = zeros(numbus*2,numParts);
for a = 1:numParts
    globalStates(areabuses{a},a) = x_k{a}(,iter);
    globalStates(numbus+areabuses{a},a) = x_k{a}(numareabus{a}+1:2*numareabus{a},iter+1);
end

for a = 1:numParts
    temp = zeros(numbus*2,1);
    globalx_k{a}(areabuses{a}) = x_k{a}(:,iter);  
end

globalx_k